// Stub for `expo/virtual/env` which is an ESM module injected by
// babel-preset-expo. Jest's `react-native` preset doesn't transform Expo
// virtual modules, so we map them to this CommonJS stub during tests.
module.exports = { env: process.env };
