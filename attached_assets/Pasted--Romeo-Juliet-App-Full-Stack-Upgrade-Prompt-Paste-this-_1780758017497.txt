# Romeo & Juliet App — Full-Stack Upgrade Prompt
### Paste this ENTIRE prompt into Replit Agent

---

## CONTEXT

This is an Expo React Native app (SDK 53, expo-router v5, TypeScript, Supabase backend, React 19, RN 0.79). The codebase already has:
- Full auth flow (welcome → referral → signin via Google OAuth)
- Onboarding (profile + photos)
- Conversation flow (ElevenLabs voice + questionnaire + archetype reveal)
- Letter flow (envelope animation → letter → match → respond → sent)
- Home + Settings screens
- Theme system: RJ_LIGHT/DARK, 4 fonts (CormorantGaramond, EBGaramond, JetBrainsMono, Caveat), 3 density levels
- Supabase hooks: useStatus(pollMs), useMatches(userId)
- Phase system: REFERRAL → PROFILE → PENDING_APPROVAL → APPROVED → CHATTING → QUESTIONNAIRE_DONE → WAITING → LETTER_READY → COMPLETE → REJECTED

**Design language:** Luxury paper/postal aesthetic. Cream backgrounds (#FBF2E3), forest green (#4E5B45), gold (#B89766), wax red (#8B3A3A), handwritten Caveat font for personal text, CormorantGaramond serif for headings. Paper noise texture overlay everywhere.

---

## TASK

Upgrade this app to a complete, polished, production-ready full-stack mobile application. Do NOT convert to web. Stay fully native Expo React Native. Complete every incomplete feature and add all upgrades below. Work in order. Do not skip steps.

---

## PART 1 — FIX BROKEN/INCOMPLETE THINGS FIRST

### 1.1 Smart Phase Router
Replace `app/index.tsx` with this routing logic:
```
no session → /(auth)/welcome
session + phase REFERRAL → /(auth)/referral
session + phase PROFILE → /(onboarding)/profile
session + phase PENDING_APPROVAL → /(onboarding)/pending
session + phase APPROVED or CHATTING → /(conversation)/voice
session + phase QUESTIONNAIRE_DONE or WAITING → /(conversation)/waiting
session + phase LETTER_READY → /(letter)/envelope
session + phase COMPLETE → /(main)/home
session + phase REJECTED → /(auth)/rejected
```
Use `useStatus(0)` (no polling, single fetch) with a loading state showing a centered pulsing WaxSeal on cream background while determining route.

### 1.2 Real Match Data
In `app/(letter)/match.tsx` and `app/(letter)/respond.tsx`:
- Replace ALL hardcoded "James — Slow Burner" data with real data from `useStatus()` and `useMatches()`
- Pull the match's first_name, archetype, photo_urls[0] from the matched user's profile
- Wire the Yes/No response to call `POST /api/match/respond` with body `{ matchId, response: 'yes'|'no', note?: string }`
- Create `lib/api.ts` function `respondToMatch(matchId, response, note?)` 

### 1.3 Fix .env.local Setup
Create `.env.local.example` with all required vars clearly documented:
```
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
EXPO_PUBLIC_WEB_BASE=https://your-rj-web-backend.vercel.app
EXPO_PUBLIC_ELEVENLABS_AGENT_ID=your_elevenlabs_agent_id
```

---

## PART 2 — ANIMATIONS & VISUAL UPGRADES

### 2.1 Particle System — Rose Petals
Create `components/effects/RosePetals.tsx`:
- Pure Reanimated 3 worklets, zero native modules
- 12 small SVG rose petal shapes, each with random: start position (top of screen), fall velocity, horizontal drift, rotation speed, opacity fade
- Trigger via `startPetals()` exported function
- Use on: archetype reveal screen (when archetype animates in), match.tsx (when match card appears), sent.tsx (confirmation)
- Petals: color #B89766 (gold) and #8B3A3A (wax red), 14x10px each, leaf shape using SVG path

### 2.2 Envelope Opening — Upgrade
In `components/letter/EnvelopeOpening.tsx`:
- After wax seal cracks: add a subtle screen flash (white, 80ms) using Animated overlay
- Add haptic: `Haptics.impactAsync(Haptics.ImpactFeedbackStyle.Medium)` on crack
- Add haptic: `Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success)` when letter rises fully
- Slow the letter rise to 1200ms (from 900ms) for more drama

### 2.3 Letter Screen Typography
In `app/(letter)/letter.tsx`:
- Wrap letter body text in Caveat font (handwritten), size 18, color ink, line-height 28
- Add a subtle page curl effect at bottom-right using a triangular shadow gradient View
- Add ink-blot decorative marks (small SVG circles, 4-6px, semi-transparent) scattered near the signature area
- Heading "Dear [firstName]," in CormorantGaramond italic size 24

### 2.4 Skeleton Loading States
Create `components/primitives/Skeleton.tsx`:
- A pulsing animated placeholder using Reanimated interpolation (opacity 0.4 → 0.9 → 0.4, 1.4s loop)
- Background: bgAlt color (#F4E9CF in light, bgAlt in dark)
- Variants: `<SkeletonLine width={number} />`, `<SkeletonBlock height={number} />`
- Use SkeletonLine in home.tsx while matches load (show 3 placeholder letter rows)
- Use SkeletonBlock in match.tsx while profile photo loads

### 2.5 Wax Seal Upgrade
In `components/primitives/WaxSeal.tsx`:
- Add a subtle radial highlight (small white circle at top-left of seal, 20% opacity) to make it look 3D/embossed
- Pulse animation should ease in-out cubic (not linear)
- Add a very faint drop shadow using RN shadow props (shadowColor waxDeep, shadowOffset 0/2, shadowOpacity 0.3, shadowRadius 4)

---

## PART 3 — NEW SCREENS & FEATURES

### 3.1 Push Notifications
Create `lib/push.ts`:
```typescript
import * as Notifications from 'expo-notifications';
import * as Device from 'expo-device';
import { supabase } from './supabase';

Notifications.setNotificationHandler({
  handleNotification: async () => ({
    shouldShowAlert: true,
    shouldPlaySound: true,
    shouldSetBadge: true,
  }),
});

export async function registerForPushNotifications(): Promise<string | null> {
  if (!Device.isDevice) return null;
  const { status: existing } = await Notifications.getPermissionsAsync();
  let finalStatus = existing;
  if (existing !== 'granted') {
    const { status } = await Notifications.requestPermissionsAsync();
    finalStatus = status;
  }
  if (finalStatus !== 'granted') return null;
  const token = (await Notifications.getExpoPushTokenAsync()).data;
  // Store token on profile
  const { data: { user } } = await supabase.auth.getUser();
  if (user) {
    await supabase.from('profiles').update({ expo_push_token: token }).eq('user_id', user.id);
  }
  return token;
}
```

Install: `expo install expo-notifications expo-device`

Call `registerForPushNotifications()` in `app/(onboarding)/pending.tsx` after profile submit.

Wire deep link: when user taps push notification for LETTER_READY, navigate to `/(letter)/envelope`.

### 3.2 Archetype Compatibility Screen
After archetype reveal in `app/(conversation)/archetype.tsx`, add a "See your matches" button that navigates to new screen `app/(conversation)/compatibility.tsx`:

Create `app/(conversation)/compatibility.tsx`:
- Header: "Who you harmonise with" in CormorantGaramond 28px italic
- Show a radial/circular layout of all 7 archetype icons arranged in a circle
- User's archetype in center, larger
- Animate each archetype icon flying outward from center on mount (Reanimated staggered, 80ms between each)
- Archetypes that "harmonise" with user: full opacity, gold border ring
- Non-harmonising archetypes: 40% opacity
- Harmonisation data (hardcode based on archetype):
  ```
  curious → [intellectual, playful, magnetic]
  grounded → [romantic, slow, curious]
  intellectual → [curious, grounded, slow]
  magnetic → [romantic, playful, curious]
  playful → [magnetic, curious, grounded]
  romantic → [grounded, magnetic, slow]
  slow → [grounded, intellectual, romantic]
  ```
- Bottom: PrimaryButton "Continue" → navigates to `/(conversation)/waiting`

### 3.3 Letter Reply Composer Upgrade
Completely replace `app/(letter)/respond.tsx`:
- Full-screen paper background (cream, PaperNoise overlay)
- Header: "Your reply to [matchName]" in CormorantGaramond italic
- Large TextInput with: Caveat font size 18, multiline, minHeight 200, paper background, no border (borderless feels like writing on paper), placeholder "Write freely..." in inkMuted color
- Character counter: bottom right, MonoLabel, shows "X / 500"
- YES / NO choice: two elegant stamp-style buttons (NOT modern buttons) — styled like postal approval stamps, forest green border for YES, wax red for NO
- "Seal & Send" button: PrimaryButton with WaxSeal icon on left, triggers wax seal press animation (quick scale 0.95 → 1.05 → 1.0, 400ms) before submitting
- On submit: calls `respondToMatch()` then navigates to `/(letter)/sent`

### 3.4 Match Timeline Screen
Create `app/(main)/timeline.tsx`:
- Accessible from home.tsx via tapping a match card
- Shows the full letter exchange history for that match as a vertical scroll
- Each letter is a paper card: cream background, subtle border, timestamp in MonoLabel, author name in CormorantGaramond, body in EBGaramond
- Romeo letters: slightly right-aligned with forest left border strip
- User's replies: slightly left-aligned with gold left border strip
- Empty state: single centered PostmarkStamp + text "Your correspondence begins here."
- Fetch data: query `letters` table OR use match data from useMatches() — if no letters table exists, gracefully show empty state

### 3.5 Biometric Lock (Optional Feature)
Create `lib/biometric.ts`:
```typescript
import * as LocalAuthentication from 'expo-local-authentication';

export async function isBiometricAvailable(): Promise<boolean> {
  const compatible = await LocalAuthentication.hasHardwareAsync();
  const enrolled = await LocalAuthentication.isEnrolledAsync();
  return compatible && enrolled;
}

export async function authenticateWithBiometric(): Promise<boolean> {
  const result = await LocalAuthentication.authenticateAsync({
    promptMessage: 'Unlock Romeo & Juliet',
    fallbackLabel: 'Use Passcode',
  });
  return result.success;
}
```

Install: `expo install expo-local-authentication`

In Settings: add toggle "Require Face ID / Fingerprint" stored in preferences. If enabled, check biometric on every app foreground event in `app/_layout.tsx` using `AppState` listener.

---

## PART 4 — BACKEND UPGRADES

### 4.1 New API Route — Match Respond
Create the API handler (this is called by the mobile app, lives in the web backend at WEB_BASE):

Document in `lib/api.ts` as:
```typescript
export async function respondToMatch(
  matchId: string,
  response: 'yes' | 'no',
  note?: string
): Promise<{ ok: boolean; error?: string }> {
  const res = await authedFetch('/api/match/respond', {
    method: 'POST',
    body: JSON.stringify({ matchId, response, note }),
  });
  if (!res.ok) {
    let error = 'Could not send response';
    try { error = (await res.json()).error ?? error; } catch {}
    return { ok: false, error };
  }
  return { ok: true };
}
```

### 4.2 Supabase Realtime Subscription
In `lib/hooks.ts`, upgrade `useStatus()`:
- Replace polling with Supabase Realtime channel subscription on `profiles` table for user's row
- `supabase.channel('profile-changes').on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'profiles', filter: `user_id=eq.${userId}` }, callback)`
- Fall back to polling (5s) if realtime subscription fails
- This makes phase transitions instant (no more waiting for poll interval)

### 4.3 Offline Detection
Install: `expo install @react-native-community/netinfo`

Create `components/primitives/OfflineBanner.tsx`:
- Uses `useNetInfo()` from @react-native-community/netinfo
- When offline: slides down a thin banner (height 36px) from top of screen
- Banner text: "Postmaster unavailable — reconnecting" in MonoLabel
- Background: bgSunken color, text inkMuted
- Animated: translateY -36 → 0 when offline, reverse when back online
- Add `<OfflineBanner />` in `app/_layout.tsx` so it appears on every screen

---

## PART 5 — POLISH & SETTINGS UPGRADES

### 5.1 Settings Screen — Add New Options
In `app/(main)/settings.tsx`, add these sections:

**Notifications section:**
- Toggle: "Letter arrived" notifications (on/off)
- Toggle: "New match found" notifications (on/off)
- Store in preferences via expo-secure-store

**Privacy section:**
- Toggle: "Require biometric to open app" (uses lib/biometric.ts)

**Account section:**
- "Delete Account" button — styled in danger color (#8B3A3A), shows Alert confirmation before calling Supabase delete

**App section:**
- Version number (read from expo-constants Constants.expoConfig.version)
- "Rate on App Store" link (opens expo-web-browser to app store URL)

### 5.2 Home Screen — Upgrade
In `app/(main)/home.tsx`:
- Add a greeting based on time of day: "Good morning," / "Good afternoon," / "Good evening," in CormorantGaramond italic above the user's first name
- If no matches yet: show an elegant empty state — large OrnamentDivider, PostmarkStamp, text "Romeo is seeking you." centered, MonoLabel below "Your letter will arrive in due time."
- Add pull-to-refresh: `<ScrollView refreshControl={<RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor={c.gold} />}>` — calls refresh() from useMatches()
- Tap on any match letter card → navigate to `/(main)/timeline` with matchId param

### 5.3 Profile Screen — Photo Drag Reorder
In `app/(onboarding)/profile.tsx`:
- Add ability to reorder photos by long-press drag using react-native-gesture-handler's `GestureDetector` + Reanimated shared values
- Show "Primary photo" badge (MonoLabel, gold color) below the first slot
- After reorder, update the photoUrls array order before submitting

### 5.4 Share Archetype Card
In `app/(conversation)/archetype.tsx`:
- Install: `expo install react-native-view-shot`
- Add a share button (IconBtn with share icon) in top-right
- On press: capture the archetype card area using `captureRef()`, then call `Share.share({ url: fileUri })` from react-native
- The captured card shows: archetype image, archetype name, user's first name, app name in corner

---

## PART 6 — PERFORMANCE & QUALITY

### 6.1 Image Optimization
In all screens using Image:
- Add `contentFit="cover"` and `transition={300}` to expo-image (if not already using expo-image, replace all `<Image>` from react-native with `<Image>` from `expo-image`)
- Install: `expo install expo-image`
- Add `cachePolicy="memory-disk"` to archetype images and Juliet portrait

### 6.2 Error Boundary
Create `components/ErrorBoundary.tsx`:
- Class component extending React.Component with componentDidCatch
- On error: show a cream paper screen with OrnamentDivider, text "Something went wrong. Our postmaster has been notified.", and a "Return to start" button that resets navigation to index
- Wrap `app/_layout.tsx` root in ErrorBoundary

### 6.3 Loading Performance
In `app/_layout.tsx`:
- Use `expo-splash-screen` `SplashScreen.preventAutoHideAsync()` + hide only after fonts loaded AND initial auth check complete
- This prevents white flash on startup

### 6.4 TypeScript Strictness
In `tsconfig.json`, ensure:
```json
{
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true
  }
}
```
Fix any resulting TS errors (use optional chaining `?.` and nullish coalescing `??`).

---

## PART 7 — TESTING

### 7.1 Add Tests for New Screens
Create test files:
- `app/(conversation)/__tests__/archetype.test.tsx` — renders archetype card, checks name displayed
- `app/(letter)/__tests__/respond.test.tsx` — renders respond screen, checks Yes/No buttons present
- `app/(main)/__tests__/timeline.test.tsx` — renders timeline, checks empty state message

---

## CONSTRAINTS

1. **No web conversion** — this stays 100% Expo React Native. No Next.js, no Vite, no browser target.
2. **Do not change the theme system** — keep RJ_LIGHT/DARK/FONTS/DENSITY tokens as-is.
3. **Do not add new fonts** — only use the 4 already bundled (CormorantGaramond, EBGaramond, JetBrainsMono, Caveat).
4. **Do not replace Supabase** — keep all existing Supabase queries working.
5. **Keep existing component names** — only extend, don't rename primitives.
6. **Install packages with `expo install` not `npm install`** — respects SDK compatibility.
7. **Every new screen must use `useRJTheme()`** — no hardcoded hex colors in components.
8. **Reanimated animations must use `useNativeDriver: true`** or Reanimated 3 worklets — no JS-thread animations.
9. **After completing ALL parts**, run `npx tsc --noEmit` and fix all TypeScript errors before finishing.
10. **After TypeScript clean**, run `npm test` — all tests must pass.

---

## DONE WHEN

- [ ] app/index.tsx routes correctly based on phase
- [ ] Match screen shows real data (not James placeholder)  
- [ ] Rose petal particles appear on archetype reveal and match acceptance
- [ ] Skeleton loading states visible while data fetches
- [ ] Push notifications registered on pending screen
- [ ] Compatibility wheel screen exists and animates
- [ ] Respond screen has full composer with stamp-style Yes/No
- [ ] Timeline screen exists and shows letter history
- [ ] Offline banner slides in when no internet
- [ ] Settings has notifications + biometric + delete account
- [ ] Home screen has greeting + pull-to-refresh + empty state
- [ ] Share archetype card works
- [ ] TypeScript: zero errors
- [ ] Tests: all passing